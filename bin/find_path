#!/bin/bash
IFS_SAVE="${IFS}"
export IFS=":"
[ -z "${1}" ] && exit 1
for dir in $PATH
do
   if [ -x "${dir}/${1}" ];then
      echo "${dir}/${1}"
      export IFS="${IFS_SAVE}"
      exit 0
   fi
done
export IFS="${IFS_SAVE}"
echo ${1} not found
exit 1
